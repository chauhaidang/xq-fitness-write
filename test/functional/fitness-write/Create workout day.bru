meta {
  name: Create workout day
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/xq-fitness-write-service/api/v1/workout-days
  body: json
  auth: inherit
}

body:json {
  {
    "routineId": 1,
    "dayNumber": 1,
    "dayName": "Push Day",
    "notes": "Focus on chest and shoulders"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
