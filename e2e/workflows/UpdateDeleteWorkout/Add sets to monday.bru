meta {
  name: Add sets to monday
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/workout-day-sets
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{authToken}}
}

body:json {
  {
    "workoutDayId": {{dayId1}},
    "muscleGroupId": 2,
    "numberOfSets": 8
  }
}

script:post-response {
  const { validateResponse, storeValue } = require('../helpers/validators.js');
  
  validateResponse(res, 201, {
    id: 'number',
    workoutDayId: 'number'
  });
  
  storeValue(bru, 'setId', res, 'id');
}

tests {
  test('Sets created', function () {
    const chai = require('chai');
    expect(res.getStatus()).to.equal(201);
  });
}
